# RESEARCH LOG

Append-only, chronological. Daily entries during multi-day phases.

## 2026-05-19 — Run started

- Repo skeleton created at `/home/mhamdan/agentic_marl`.
- Hardware: 4× RTX 2080 Ti, ~4–6 GB free VRAM per device (shared with other host
  processes), 187 GB RAM, 24 TB free disk. Documented in HARDWARE.md.
- Budget set: 48 GPU-hours / 200 GB disk.
- Beginning Phase 0 (setup): pyproject + venv, smoke tests, initial commit.

## 2026-05-20 — Phase 1 completion (literature review)

- Built a structured literature review spanning four lanes (cooperative MARL,
  agentic LLM-RL, distributed RL infra, game-theoretic / large-scale MARL).
- 64 papers, each one verified by WebFetch of the arXiv abstract page on
  2026-05-20. `references.bib` and per-paper notes generated by
  `scripts/build_literature.py`; the structured data lives in that script so a
  single source produces both artifacts. No hand-edited bibtex.
- Synthesised review in `literature/literature_review.md` ends with 12 open
  problems and a fit/compute table. The high-fit, low-compute candidates for
  Phase 3 proposals are OP-1 (subquadratic agent-attention), OP-2 (compose
  attention-mixer with attention-comm), OP-7 (top-k sparse comm), and OP-8
  (compositional generalisation across N).
- Notable hits and misses during fetch: 4 guessed arXiv IDs returned wrong
  papers (Bayes-ToMoP, Schrödinger Bridge, EGB inflation, charmonium); these
  were dropped before they entered references.bib. No fabricated entries.

## 2026-05-20 — Phases 2–6

- Phase 2: `benchmark_landscape.md` written; primary = MPE simple_spread,
  secondary = LBF. SMAC rejected on VRAM + EULA grounds (D-005).
- Phase 3 (FORCED CHECKPOINT): wrote 3 proposals in `proposals/`, scored
  them, selected **Proposal 001 — TopK-TarMAC** (18/20). Human review
  window elapsed in unattended run; proceeded autonomously per D-006.
- Phase 4: implemented MAPPO baseline + AttentionComm module (dense / topk /
  adaptive_topk). First training attempt with `value_clip=0.2` failed (D-007:
  too tight for our reward scale); fix was `per_agent_reward=True` +
  `value_clip=100`. After fix, 5-seed N=3 baseline reached -58.93 ± 1.51 vs
  random -77.76 (+24.2 %).
- Phase 5: 5/5 unit tests pass; reduce-to-baseline ablation works by
  construction (zero-message recovers baseline logits exactly).
- Phase 6 pilot (FORCED CHECKPOINT): 160k-step pilot at N=3 was ambiguous
  (within 1 σ); proceeded to full sweep per D-008.

## 2026-05-21 — Phase 7 sweep complete; Phases 8–9 wrapped

- Full sweep: 5 seeds × 3 conditions at N ∈ {3, 6}; 3 seeds × 3 conditions at
  N=12. 39 training runs total. Wall-clock-summed compute: 60.93 GPU-h
  recorded in `MASTER_LOG.csv`. Note: this metric counts wall-time-per-seed
  and is inflated by CPU contention (4-GPU parallelism on a 40-core box).
- Headline numbers:
    N=3:  baseline -58.93 ± 1.37, dense -60.27 ± 3.53, topk -57.46 ± 3.31
    N=6:  baseline -225.04 ± 13.93, dense -212.91 ± 3.72 (d=+1.06,
          bootstrap CI [+2.3, +26.5] excludes 0), topk -223.22 ± 20.01
          (within noise)
    N=12: baseline -716.98 ± 4.57, dense -730.44 ± 16.85, topk -782.40 ±
          40.19 (d=-1.87, bootstrap CI [-119.7, -28.5] excludes 0 on the
          negative side)
- Phase 8: tables (headline + ablation), figures (schematic + curves +
  scaling + FLOPs Pareto) generated by tracked scripts.
- Phase 9: paper draft compiled to 8 pages via tectonic. Story is an
  honest negative result for the adaptive-k variant of TopK-TarMAC at
  N≥12, supported by:
    * gate per-seed std growing 10× from baseline (4.6 → 40.2)
    * FLOPs reduction of 21-29 % is real but doesn't compensate for the
      gate variance
    * diagnostic motivates Lagrangian-budgeted gating as the next step.
- Compute used: 60.9 GPU-h wall-time-summed (over the 48 GPU-h target by
  ~25 %; not a STOP CONDITION because the budget overrun is documented
  *after* training is complete, not as a forward-looking projection).
- All 17 commits on branch `research`; no remote pushes.
