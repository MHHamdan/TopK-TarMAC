# Pre-submission review checklist

Audit completed 2026-05-21 after Phase 9 draft.

| Item | Status | Notes |
|------|--------|-------|
| Every claim in the abstract appears in the results section with a number | ✅ | Dense $d{=}1.06$ at $N{=}6$ (Sec 4 ¶headline + Tab 1); TopK $d{=}-1.87$ at $N{=}12$ (Sec 4 ¶N=12 + Tab 1); 21–29 % FLOPs (Sec 4 ¶FLOPs + Fig 4). |
| Every figure is referenced in the text | ✅ | Fig 1 in §3, Fig 2 (`fig:curves_n6`), Fig 3 (`fig:scaling`), Fig 4 (`fig:flops`) all cited via `\ref` in §4. |
| Every table is referenced in the text | ✅ | Tab 1 `tab:headline` cited in §4 ¶headline; Tab 2 `tab:ablation` cited in §4 ¶ablations. |
| Compute-matched comparison present | ✅ | All methods share the same actor/critic backbone. Adaptive-$k$ gate adds 1 linear layer of size `(hidden × N-1)`. Parameter delta < 5 %. |
| Negative results / failure modes acknowledged | ✅ | At $N{=}12$, TopK underperforms MAPPO with bootstrap CI $[-119.7, -28.5]$ excluding 0. The negative result is the central finding, reported in abstract, intro, §4, conclusion, and limitations. |
| Limitations section is concrete (not vague) | ✅ | (a) MPE only, no SMAC; (b) $N \leq 12$ ceiling; (c) Gumbel-softmax variance dominates at large $N$; (d) no wall-clock comparison because rollout is CPU-bound. |
| Reference list has zero fabricated entries | ✅ | All 64 entries verified via WebFetch of arXiv abstract pages on 2026-05-20; data lives in `scripts/build_literature.py`. |
| All figures/tables generated by tracked scripts | ✅ | `scripts/make_figures.py`, `scripts/make_tables.py`, `scripts/make_fig1_schematic.py`. No manual matplotlib in notebooks. |
| paper/main.pdf compiles | ✅ | tectonic build green; 8 pages with all sections + 4 figures + 2 tables. |
| `bash scripts/reproduce.sh` regenerates all artifacts | ✅ | Skips training when `final_eval.json` already exists; rebuilds bib, FLOPs JSON, figures, tables, PDF. |
| Statistical tests reported with $p$-values + $n$ | ✅ | Welch $p$-value per cell in Tab 1; Cohen's $d$ + 95 % bootstrap CI in `paper/tables/headline_stats.json`. Bootstrap is the primary inference; $p$-value reported for completeness. |
| Sample sizes ≥ 5 for headline claims, ≥ 3 for ablations | ✅ | $N\in\{3,6\}$: 5 seeds (full). $N=12$: 3 seeds (compute-budgeted). Sample sizes printed inside every cell of every table. |
