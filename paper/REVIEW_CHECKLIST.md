# Pre-submission review checklist

Filled in after Phase 9.

| Item | Status | Notes |
|------|--------|-------|
| Every claim in the abstract appears in the results section with a number | ☐ | After table_headline.tex is filled, audit abstract claims line-by-line. |
| Every figure is referenced in the text | ☐ | Currently fig1 (schematic), fig2 (curves), fig3 (scaling), fig4 (FLOPs Pareto). All referenced via `\ref{fig:scaling}` and `\ref{fig:flops}` placeholders in 04_experiments.tex. |
| Every table is referenced in the text | ☐ | table_headline.tex referenced via `\ref{tab:headline}`. |
| Compute-matched comparison present | ☐ | All methods share the same MAPPO backbone; the comm module adds <10 % parameters. Adaptive-k gate adds 1 linear layer per agent. Param counts must be reported in Section 4. |
| Negative results / failure modes acknowledged | ☐ | At N=3, no method beats baseline significantly (p=0.4–0.8); this is explicitly stated in the experiments section. Limitations section also notes the N≤12 ceiling. |
| Limitations section is concrete (not vague) | ☐ | Done: lists no SMAC, no wall-clock comparison, Gumbel-ST bias, N≤12 cap. |
| Reference list has zero fabricated entries | ✅ | All 64 entries fetched from arXiv on 2026-05-20 and verified via WebFetch. |
| All figures/tables generated by tracked scripts | ✅ | `scripts/make_figures.py`, `scripts/make_tables.py`, `scripts/make_fig1_schematic.py`. |
| paper/main.pdf compiles | ✅ | Verified with tectonic. |
| `bash scripts/reproduce.sh` regenerates all artifacts | ✅ | Tested via skipping training step. |
| Statistical tests reported with p-values + n | ☐ | Welch t-test per (N, method) printed in table cells. Audit that all "A beats B" prose lines cite a p-value. |
| Sample sizes ≥ 5 for headline claims, ≥ 3 for ablations | ☐ | N∈{3,6}: 5 seeds. N=12: 3 seeds. Need to document N=12 sample size in caption. |
